* `ocean.core.array.Iteration`

  New module containing single function for now - `reverse`, which allows to
  iterate given slice in reverse order without any allocations (similar to
  built-in `foreach_reverse`).
