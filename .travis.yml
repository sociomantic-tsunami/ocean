sudo: required

services:
    - docker

# D is still in beta and we don't even use it's features, so we just pick
# a lightweight environment
language: c

install:
    - ./docker/travis-install.sh

script:
    - ./docker/travis-script.sh
