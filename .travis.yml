sudo: required

services:
    - docker

env:
    IMAGE=v1-trusty
    IMAGE=v1-xenial

# D is still in beta and we don't even use it's features, so we just pick
# a lightweight environment
language: c

script:
    - ci/docker-run.sh
